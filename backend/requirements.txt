# Use Python 3.11 or 3.12 for this pinned dependency set.
# Python 3.14 can fail while building older native dependencies on Windows.
fastapi==0.109.1
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
asyncpg==0.29.0
aiosqlite==0.20.0
alembic==1.13.1
pydantic==2.5.3
pydantic-settings==2.1.0
# python-jose: bumped to 3.4.0 to patch PYSEC-2024-232 + PYSEC-2024-233 (F-035).
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
# python-multipart: bumped to 0.0.18 for CVE-2024-53981. Newer 0.0.22+ fixes
# additional CVEs but introduces breaking changes; revisit when bumping
# fastapi minor.
python-multipart==0.0.18
email-validator==2.1.0
greenlet==3.0.3
pytest==8.3.5
pytest-asyncio==0.25.3
httpx==0.27.2
imapclient==2.3.1
mail-parser==3.15.0
arq==0.26.1
pdfplumber==0.11.4
pytesseract==0.3.13
# Pillow: bumped to patch image-decoder CVEs (F-035). 11.x is the latest
# series compatible with this code's PIL usage.
Pillow==11.0.0
pdf2image==1.17.0
openpyxl==3.1.5
xlrd==2.0.1
python-docx==1.1.2
openai==1.51.0
# cryptography: 44.0.1 patches CVE-2024-12797. Newer 46.x adds more fixes
# but requires Python 3.12+; staying on 44.x for compatibility.
cryptography==44.0.1
chardet==5.2.0
langdetect==1.0.9
# python-holidays: source for public/statutory holiday data per country.
# Used by GET /holidays/suggestions to seed the admin's import flow.
holidays==0.59
slowapi==0.1.9
tzdata==2024.2
# starlette: CVE-2024-47874 + CVE-2025-54121 require starlette>=0.40.0
# (audit recommends 0.47.2), but fastapi 0.109.1 transitively pins
# starlette<0.36. Resolving these CVEs requires bumping fastapi to 0.115+,
# which is a non-trivial upgrade (pydantic v2 model changes, lifespan API
# changes). Deferred as a follow-up; pinning the transitive version
# explicitly so any future bump is intentional.
starlette==0.35.1
